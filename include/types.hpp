#ifndef TYPES_HPP
#define TYPES_HPP

#include <cstdint>
#include <vector>

namespace palloc {
namespace types {
using UintVector = std::vector<uint64_t>;
using DoubleVector = std::vector<double>;
}  // namespace types
}  // namespace palloc

#endif