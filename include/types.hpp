#ifndef TYPES_HPP
#define TYPES_HPP

#include <cstdint>
#include <vector>

namespace palloc {
using UintVector = std::vector<uint32_t>;
using DoubleVector = std::vector<double>;
}  // namespace palloc

#endif